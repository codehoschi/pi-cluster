apiVersion: mariadb.mmontes.io/v1alpha1
kind: Grant
metadata:
  name: bookstack
spec:
  mariaDbRef:
    name: mariadb
  privileges:
    - "CREATE"
    - "SELECT"
    - "INSERT"
    - "UPDATE"
  database: "*"
  table: "*"
  username: bookstack
